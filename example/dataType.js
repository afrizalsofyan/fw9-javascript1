const number=123 //tipe data number

const str = "abcd" // tipe data string
 
const func = function(a, b){
    return a + b
} //tipe data function

const conditon = true // tipe data boolean

const nll = null

const undf = undefined

const skill = {
    skill1: "php",
    skill2: "dart",
    skill3: "react"
}

const arr = [1,2,"abc",true, func(1,2)]